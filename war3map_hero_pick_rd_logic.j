# RD 模式英雄选择核心执行逻辑分析报告 (Function `K12`)

本报告针对 Jass 脚本中 RD 模式的轮次执行函数 `K12` 进行深度解析。该函数由 `war3map_hero_pick_rd_timer.j` 中的 20 秒周期性触发器驱动，是整个选人阶段的“执行总裁”。

---

## 1. 核心职责描述
`K12` 函数每 20 秒被触发一次，主要负责：
1. **超时检测**：检查上一轮玩家是否选择了英雄。
2. **惩罚机制**：对未选英雄的玩家执行强行随机（Force Random）。
3. **轮次切换**：宣布下一组选人玩家，并重置倒计时 UI。
4. **场景清理**：选人结束后销毁相关单位和计时器，转场进入正式游戏。

---

## 2. 关键变量与函数定义

| 变量/函数 | 类型 | 逻辑含义 |
| :--- | :--- | :--- |
| **`KO2`** | `integer` | **轮次索引**。从 0 开始，每 20 秒加 1，控制选人进度。 |
| **`K22 / K32`** | `player` | **上一轮玩家**。用于判定谁超时了。 |
| **`QM0 / QN0`** | `player` | **当前轮玩家**。本轮正在选人的操作者。 |
| **`QP0[id]`** | `boolean` | **选择标记**。记录玩家是否已手动点击确认。 |
| **`JR2()`** | `function` | **强行随机函数**。核心惩罚逻辑。 |
| **`JW2(20,...)`**| `function` | **UI重置函数**。启动 C++ 监控到的那个 20s 计时器。 |

---

## 3. 逻辑流程拆解

### 3.1 初始轮次 (KO2 = 0)
*   根据 `QS0`（随机决定的首选阵营）确定第一位选人玩家。
*   宣布选人开始，并调用 `JW2(20,...)` 开启第一个 20 秒倒计时。

### 3.2 中间轮次与超时惩罚 (KO2 = 1 ~ 5)
这是脚本最核心的迭代部分，逻辑高度一致：
*   **第一步：判定超时**
    ```jass
    if QP0[GetPlayerId(K22)] == false then
        call JR2(K22, JP2(), true) // 时间到，强行给玩家 K22 随机英雄
    endif
    ```
*   **第二步：宣布下一组**
    脚本根据阵营顺序切换 `QM0` 和 `QN0`。
*   **第三步：重置倒计时**
    每一轮末尾都会执行 `call JW2(20, ...)`。这正是你在 Hook 中观察到 `TimerGetRemaining` 频繁返回 **20.0 (0x41A00000)** 及其递减值的原因。

### 3.3 选人结束与转场 (KO2 = 8)
当所有玩家完成选择（或被强制随机）后，执行以下清理：
1.  **物理清理**：
    *   移除展示用的 24 个英雄模型 (`RemoveUnit(QA0[i])`)。
    *   对玩家的“选人精灵”设置 2 秒生命周期使其消失。
2.  **逻辑清理**：
    *   `call S0I(t)`：**销毁每 20 秒一次的循环触发器**，停止调用 `K12`。
    *   `call FlushChildHashtable`：清理存储轮次数据的哈希表。
3.  **正式开始**：
    *   创建一个新的 7 秒单次计时器，到期后执行 `JA2`（英雄正式在泉水出生）。

---

## 4. 与 C++ Hook 现象的对应关系

*   **20秒的来源**：脚本中每轮调用的 `JW2(20,...)` 内部启动了计时器。
*   **18.6s / 18.7s 采样值**：这是因为 `K12` 触发了计时器后，UI 刷新触发器以 0.1s 频率不断读取该时间，被你的插件截获。
*   **重复调用**：因为 `K12` 本身就是一个 20 秒一次的“死循环”触发器，直到 `KO2 == 8` 才被手动销毁。

---

## 5. 结论

`K12` 是典型的**状态机逻辑**。它不负责“选人”动作本身，而是负责“**监督选人过程**”。

**建议存储文件名：** `war3map_hero_pick_rd_logic.j`
**主要功能：** 处理 RD 模式的轮次演进、超时强制随机惩罚、以及选人结束后的场景清理。